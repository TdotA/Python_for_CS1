##########################################################################
# Write a function power_sum(k, n) that determines the sum of the $k$-th
# powers of the first $n$ positive integers:
# 
# $$1^k + 2^k + 3^k + \cdots + n^k.$$
#
# Example:
# 
#     >>> power_sum(10, 2)
#     1025
#     >>> power_sum(2, 10)
#     385
##########################################################################



##########################################################################
# Write a function inverted_harmonic(x) that determines the minimal
# number of terms of the harmonic series
# 
# $$1/1 + 1/2 + 1/3 + 1/4 + \cdots$$
#
# that we need to sum up in order to exceed the value of $x$.
# 
# Example:
# 
#     >>> inverted_harmonic(2)
#     4
# 
# Comment: $11/6 = 1 + 1/2 + 1/3 \leq 2 < 1 + 1/2 + 1/3 + 1/4 = 25/12$.
##########################################################################



##########################################################################
# Write a function digit_sum(n) that returns the sum of the digits of a
# positive integer $n$.
#
# Example:
# 
#     >>> digit_sum(2014)
#     7
##########################################################################



##########################################################################
# Write a function hand_angle(h, m) that determines the smaller angle
# between the hour hand and the minute hand of an analog clock. The time
# is given by arguemnts h (hours) and m (minutes). Note that each minute
# the hour hand also moves slightly. The function should return a pair of
# two numbers: angular degrees and angular minutes.
#
# Example:
# 
#     >>> hand_angle(17, 47)
#     (108, 30)
##########################################################################


